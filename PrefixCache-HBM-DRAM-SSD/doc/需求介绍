项目任务目的：02文件夹的目的是为了测试HBM Prefix Cache命中的性能基线，本项目目的是为了测试KV Cache被挤压到DRAM时的性能，对比02看下降多少。
项目环境：昇腾910C单机单卡、vllm 0.23.0、mooncake，因为是单机单卡，应该不需要RDMA
        请求序列统一16K，block-size统一128
参考：02目录是做性能测试的两个脚本，验证是OK的，本项目可以参考。

主要步骤如下，请审视合理性，帮助完善。特别步骤三如何自动化、准确检查，

一、冷启动：P1批次请求填入HBM、性能结果表格Sheet页 R1-warmup；
1、生成第一批请求P1，16K序列。批次P1共有3小批请求，分别是并发1*5，并发5*5，并发10*5；
2、冷启动P1请求，将P1请求全部缓存到HBM，记录冷启动性能R1。同时参考02文件夹的请求、记录方式；

二、HBM Cache：再次请求P1,性能结果记录到Sheet页 R2-hbm-cache；

三、挤出HBM：利用压力测试（参考manu），将P1 KV挤压到DRAM。本章节需要设计方案和脚本，自动化&准确检查P1确实已挤压到DRAM

四、DRAM Cache: 再次请求P1, 过程中检查llm输出，需确认并记录确实是DRAM取出的（如果不是DRAM需要排除结果）, 性能结果记录到sheet页 R3-dram-cache;
